# Lessons

The lesson content is embedded in `script.js` so the app works even when opened directly from `file://`.

If you want to externalize lessons later, this folder is the intended place for per-lesson JSON/Markdown files.

